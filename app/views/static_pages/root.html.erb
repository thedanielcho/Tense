<% if logged_in? %>
  <script type="text/javascript">
    window.currentUser = <%= render(
      "api/users/user.json.jbuilder",
      user: current_user
    ).html_safe %>
    window.currentChannels = <%= render(
      "api/channels/channels.json.jbuilder",
      channels: Channel.all
    ).html_safe %>
  </script>
<% end %>
<script type="text/javascript">
    window.currentChannels = <%= render(
      "api/channels/channels.json.jbuilder",
      channels: Channel.all
    ).html_safe %>
</script>
<main id="root"></main>